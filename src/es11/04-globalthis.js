// El objeto window si existe y cuenta con parámetros en consola del navegador.
console.log(window);// Res: is not defined. Del lado del navegador.
console.log(global);// Del lado de node.
console.log(selft);// Del lado de webworker.
console.log(globalThis);// Funciona Global para cualquier tipo de app javascript.
