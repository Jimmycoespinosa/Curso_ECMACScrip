let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world + '!';
//console.log(epicPhrase);

// Template Literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi-line Strings
let lorem = 'Esto es un string \n' + 'esta es otra línea';
let lorem2 = `Esta es una frase épica
la continuación de esta fráse épica`;
console.log(lorem2);

